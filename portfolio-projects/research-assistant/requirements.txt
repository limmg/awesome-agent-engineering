# ── 核心框架 ──────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0          # ChatZhipuAI
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0  # SqliteSaver 跨重启持久化
aiosqlite>=0.19.0                   # AsyncSqliteSaver 依赖（async 图必须）

# ── LLM & 检索 ───────────────────────────────────────────
zhipuai>=2.1.0                      # 智谱 API
ddgs>=9.0.0                         # 联网搜索（duckduckgo_search 已更名）

# ── 服务化（阶段 3）──────────────────────────────────────
fastapi>=0.100.0
uvicorn>=0.20.0
sse-starlette>=1.6.0                # SSE 流式响应

# ── 配置 & 数据 ──────────────────────────────────────────
pydantic>=2.0
pydantic-settings>=2.0              # 从 .env 读配置
python-dotenv>=1.0.0

# ── 测试（阶段 4）────────────────────────────────────────
pytest>=7.0.0
pytest-asyncio>=0.21.0

# ── MCP 集成（LLMOps L09，可选）─────────────────────────────
# 作为 MCP client 调用 kb-qa 知识库 server（src/research_assistant/kb_mcp_client.py）。
# 只有开启 enable_kb_search 时才需要；核心研究流程不依赖。
mcp>=1.2.0
