zhipuai>=2.1.0
chromadb>=0.5.0
python-dotenv>=1.0.0
sniffio>=1.3.0  # zhipuai 的间接依赖，部分版本未正确声明

# ---- Lesson 02 起新增 ----
matplotlib>=3.7.0   # 画图：相似度热力图、向量降维散点图
scikit-learn>=1.3.0 # 降维：PCA 把高维向量投影到 2D 平面可视化

# ---- Lesson 04 起新增 ----
langchain-text-splitters>=0.1.0  # 文档切块工具（按语义分隔符智能切分）

# ---- Lesson 06 起新增 ----
rank-bm25>=0.2.2  # BM25 关键词检索（混合检索的另一半）

# ---- Agent L09 起新增 ----
duckduckgo-search>=6.0.0  # 免费联网搜索（毕业项目用，无需 API key）

# ---- 框架进阶课程 起新增（framework-lessons/）----
# LangChain 1.x：LCEL 管道、Prompt、OutputParser（L01-L05）
langchain>=0.3.0
langchain-core>=0.3.0          # Runnable 接口、LCEL 基类（抽象根基）
langchain-community>=0.3.0     # ChatZhipuAI / ZhipuAIEmbeddings（⚠️ 正被 sunset，见 L01 README）
langchain-chroma>=0.1.0        # Chroma 独立集成包（新写法）
# LangGraph：状态化 Agent 编排（L06-L09）
langgraph>=0.2.0
pydantic>=2.0                  # 结构化输出（L02 起用 PydanticOutputParser）

# ---- 课程七 GUI Agent 起新增（gui-agent-lessons/）----
# 浏览器自动化：L01 起 BrowserSession 控制，L09 落地 async 版接 research-assistant
playwright>=1.40.0
# 浏览器二进制单独装（国内镜像，只装 chromium）：
#   PLAYWRIGHT_DOWNLOAD_HOST=https://cdn.npmmirror.com/binaries/playwright \
#     python -m playwright install chromium
pillow>=10.0.0                 # L05 视觉路线：截图 SoM 标注（画框编号）

# ---- 多模态文档智能课程 起新增（doc-intelligence-lessons/）----
# PDF 解析：L00 毒文档生成 + L01 版面解析 + L06 区域裁剪（PyMuPDF/fitz，CPU、pip 友好）
PyMuPDF>=1.24.0
# PDF 表格抽取：L02 复杂表格结构化（pdfplumber，CPU、pip 友好）
pdfplumber>=0.11.0
# PDF 组版（备选）：L00 毒文档生成的备选引擎（reportlab；fpdf2 亦可，二选一）
reportlab>=4.0.0
# 本地 OCR：L03 扫描件三路线（RapidOCR，onnx 轻量、CPU、pip 友好；首次运行会下 onnx 模型）
rapidocr_onnxruntime>=1.3.0   # L03 落地：置信度路由的本地 OCR 引擎
# 语音入口：L07 ASR + TTS（faster-whisper CPU small 模型 + edge-tts 免费；仅 L07 需要）
faster-whisper>=1.0.0         # L07 ASR：语音转文字（CTranslate2 后端，比 OpenAI whisper 轻）
edge-tts>=6.1.0               # L07 TTS：文字转语音（微软边缘服务，免费）

